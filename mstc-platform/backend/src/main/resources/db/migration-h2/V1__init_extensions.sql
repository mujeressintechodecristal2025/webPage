-- ============================================================
-- V1 — H2 compatible (no extensions needed)
-- H2 genera UUIDs nativamente con RANDOM_UUID()
-- ============================================================

-- No-op: H2 no necesita extensiones
